version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - rm -rf node_modules package-lock.json
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .nuxt/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
